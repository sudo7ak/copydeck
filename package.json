{
  "name": "copydeck",
  "version": "1.0.0",
  "description": "",
  "main": "background.js",
  "scripts": {
    "test": "npx playwright test test-extension.spec.js",
    "lint": "eslint . -f json -o eslint-report.json",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write '**/*.{js,json,html,css,md}'",
    "format:check": "prettier --check '**/*.{js,json,html,css,md}'",
    "package": "node scripts/package-extension.js",
    "test:all": "npx playwright test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "@playwright/test": "^1.58.2",
    "playwright": "^1.58.2"
  },
  "devDependencies": {
    "eslint": "^10.1.0",
    "eslint-config-prettier": "^10.1.8",
    "prettier": "^3.8.1"
  }
}
